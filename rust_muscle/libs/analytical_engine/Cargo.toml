[package]
name = "analytical_engine"
version = "0.1.0"
edition = "2024"

[dependencies]
polars = { version = "0.37", features = ["lazy", "csv", "json", "parquet", "dtype-struct", "strings", "regex", "pivot"] }
serde_json = "1.0"
